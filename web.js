import {
  BroadcastChannelFeed,
  MessageChannelFeed,
  Feed,
  eventData,
} from "./index";

(function (window) {
  window.feed = {
    BroadcastChannelFeed,
    MessageChannelFeed,
    Feed,
    eventData,
  }
})(window)
